[package]
name = "v0id_tfhe_cuda"
version = "0.1.0"
edition = "2021"
rust-version = "1.91.1"
publish = false
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
tfhe = { version = "=1.6.1", features = ["integer", "gpu"] }
